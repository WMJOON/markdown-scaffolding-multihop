#!/usr/bin/env bash
# Legacy CLI wrapper for msm-record-archive.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CANONICAL="$SCRIPT_DIR/../../msm-record-archive/scripts/msm-record-archive"

exec "$CANONICAL" "$@"
