# msm-obsidian-projection Legacy Core

`msm-obsidian-projection` is a compatibility shim.
The implementation and documentation moved to `../msm-explain/`.

Use:

```bash
scripts/msm-explain run --target REPO --domain instance --apply
scripts/msm-explain list --target REPO
```
