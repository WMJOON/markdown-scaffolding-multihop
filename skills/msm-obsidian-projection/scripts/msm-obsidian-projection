#!/usr/bin/env bash
# Legacy CLI wrapper for msm-explain.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CANONICAL="$SCRIPT_DIR/../../msm-explain/scripts/msm-explain"

exec "$CANONICAL" "$@"
